spring:
   profiles:
      active:
        - beta
---
server:
  port: 8201

spring:
  profiles: dev
  application:
      name: microconfig-test-client-dev

eureka:
 client:
  service-url:
    defaultZone: http://localhost:7001/eureka

---

server:
  port: 8102

spring:
  profiles: beta
  application:
      name: microconfig-test-client-b

eureka:
 client:
  service-url:
    defaultZone: http://localhost:7001/eureka
